-- Character: "Possess"
-- Character.Subscribe("Possess", function(_, pPlayer)
--     if not LocalPlayer() or (pPlayer ~= LocalPlayer()) then return end

    --TODO: Remove workaround when fixed in game
    -- Timer.SetTimeout(function()
        -- Effects
        -- PostProcess.SetPPBloom(0.5, 5)
        -- World.SetPPChromaticAberration( 1, 0.001 )
        -- PostProcess.SetPPImageEffects(0.5, 1.02)

        -- Color correction
        -- PostProcess.SetFilm(0.8, 0.55, 0.26, 0.001, 0.3)
        -- PostProcess.SetPPGlobalSaturation(Color(0.8, 0.8, 1))
    -- end, 0)
-- end)
